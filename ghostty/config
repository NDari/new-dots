# Font
font-family = JetBrainsMonoNL Nerd Font
font-style = Bold
font-style-bold = Bold
font-style-italic = Bold
font-style-bold-italic = Bold
font-size = 12

# Window
window-padding-balance = true
window-decoration = auto
window-theme = dark
window-height = 50
window-width = 200

# Colors (Gruvbox dark)
# theme = Gruvbox Dark Hard
theme = Catppuccin Frappe

# Cursor
cursor-style = block
cursor-style-blink = false
cursor-opacity = 1.0

# Scrolling
scrollback-limit = 10000
mouse-scroll-multiplier = 3

# Mouse
mouse-hide-while-typing = true
copy-on-select = true

# Keybinds, general
keybind = shift+enter=text:\x1b\x0d

# nav
keybind = ctrl+h=goto_split:left
keybind = ctrl+l=goto_split:right
keybind = ctrl+j=goto_split:bottom
keybind = ctrl+k=goto_split:top

# Tmux-like Keybindings (Prefix: Ctrl+S)
# New window (Ghostty uses tabs for windows)
keybind = ctrl+s>c=new_tab

# Split panes
# '\' for horizontal split (side-by-side)
keybind = ctrl+s>\=new_split:right
# '-' for vertical split (top and bottom)
keybind = ctrl+s>-=new_split:down

# Reload Ghostty configuration
keybind = ctrl+s>r=reload_config

# Optional: Toggle split zoom (Tmux default is Prefix + z)
keybind = ctrl+s>z=toggle_split_zoom

# Optional: Close the current split or tab (Tmux default is Prefix + x)
keybind = ctrl+s>x=close_surface
